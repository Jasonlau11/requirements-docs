# 客户发布订单与审核产品需求说明书

## 需求概览

### 核心摘要
本需求将客户「发单—审核—发布」的完整链路讲清楚、落下去：客户按**任务类型**（前 7 种）可选**订单模板**、选**一种创建模式**（手工/上传文档 AI/自然语言 AI）填写或生成订单内容，必填**结算模式**、**订单交付模式**、**订单报价**、**接单要求**及**响应时限/招标时限**，并设置**订单属性与接单方式**（私密或公开、指定接单方/运营方接单/公开抢单及超时行为）后提交；运营审核通过则按属性与接单方式发布（上架订单广场或对指定方开放），不通过则打回并反馈原因、客户可见。我们通过**结算模式二选一**明确里程碑与结算由客户还是运营方定义，通过**响应/招标时限与超时行为可配置**（自动下架或转为公开抢单/自动隐藏）并在待审核与推广中阶段支持客户灵活调整时限，让发单流程标准化、审核可追溯、发布结果可预期。客户与运营会感受到：发单有章可循、审核结果清晰、订单按设定方式正确发布与展示。

### 设计思路
设计上坚持「流程固定、必填统一、属性与接单方式一致、审核结果可追溯」：发单顺序与整体方案一致（类型→模板→模式→内容→必填→属性与接单方式→提交），不扩展未提及的步骤；必填信息（结算模式、订单交付模式、订单报价、接单要求、响应/招标时限）与《订单发布模板与任务类型管理》中的模板字段统一配置，避免重复定义；订单性质（私密/公开）与接单方式（指定接单方、运营方接单、公开抢单）的组合严格按方案约束，超时行为（指定接单方未响应/拒绝、公开抢单超时）可配置且与状态机联动；审核打回必填原因且客户可见，审核通过后系统按接单方式自动处理发布，与订单广场、指定给我的订单、运营承接等后续能力衔接。

### 历史实现参考
在设计过程中，我们严格依据《CSDN 订单管理系统整体方案》第 6.3 节「客户发布订单与审核」及第 4.1 节「订单发布与审核」「订单状态流转」的表述进行拆分，发单流程、订单必填信息、结算模式、响应时限与招标时限、上架状态与超时行为、订单属性与接单方式、订单状态与流转及审核操作的描述与方案一致。同时参考了同项目《订单发布模板与任务类型管理产品需求说明书》中任务类型与模板选择、三种创建模式与模板结合、必含字段（订单交付模式、订单报价、接单要求）及校验规则的设计，本需求中订单内容字段与模板字段统一，不重复定义格式与校验，保持与模板需求的一致性。本项目 `docs/history/` 下未发现「客户发布订单与审核」专项历史需求文档，以整体方案及同项目模板需求为设计来源；文档结构与验收准则（Given-When-Then 与 Gherkin）格式与同项目《用户注册与身份认证》《订单发布模板与任务类型管理》需求说明书保持一致。

---

# 第1章：概述

## 1.1 术语表

| 术语 | 英文（如需） | 描述 |
|------|--------------|------|
| 客户 | Order Publisher / Requester | 在订单平台上发布订单的企业或组织；发布订单时选择任务类型（前 7 种之一）、可选订单模板、一种创建模式，填写必填信息并设置订单属性与接单方式后提交审核。 |
| 运营方 | Reviewer / Operation | CSDN 运营人员，负责对客户提交的订单进行审核（通过/打回）；打回时需填写原因，客户可见。审核通过后系统按订单属性与接单方式处理发布。 |
| 任务类型 | Task Type | 客户发单时可选的订单类型，**客户端可选前 7 种**：文本创作、视频创作、软件开发、产品评测、营销推广、平面设计、硬件订单；由运营在后台配置并关联发布模板。 |
| 订单模板 | Publish Template | 与任务类型对应的订单填写模板；客户可选该类型下的某模板，再选择一种创建模式填写或生成订单内容。 |
| 订单创建模式 | Order Creation Mode | 三种方式之一：**手工创建**（按表单项逐项填写）、**上传文档由 AI 解析创建**（上传文档后 AI 生成草稿）、**自然语言 AI 解析创建**（自然语言描述后 AI 生成草稿）；均可与订单模板结合。 |
| 结算模式 | Settlement Mode | 客户定义订单时二选一：**平台抽佣**（客户与用户直接交易结算，由客户定义该订单的里程碑、结算等）或**平台承包转包**（客户将订单承包给平台、平台转包给用户，由平台运营方定义该订单的里程碑、结算等）。 |
| 订单交付模式 | Order Delivery Mode | 客户发布订单时必填项之一，描述或选择该订单的交付方式；具体枚举与含义与发布模板字段一致或在需求中定义。 |
| 订单报价 | Order Quote / Price | 客户发布订单时必填项之一，表示该订单的预算或报价信息。 |
| 接单要求 | Contractor Requirements | 客户发布订单时必填项之一，描述对接单方的资格、能力或资质要求。 |
| 响应时限 | Response Deadline | **指定接单方**订单的必配项：被指定方需在该时限内确认接单或拒绝；支持常用预设（如 3 天、7 天、15 天）或**无限制**；超时未响应或拒绝时按订单配置执行自动下架或自动转为公开抢单。 |
| 招标时限 | Bidding Deadline | **公开抢单**订单的必配项：订单在订单广场接受报名的时长；支持常用预设或**无限制**；超时后按配置自动下架或超时订单自动隐藏。 |
| 订单性质 | Order Nature / Visibility | **私密订单**（不在订单广场公开展示）或**公开订单**（可在订单广场展示）；决定接单方式可选范围。 |
| 订单属性 | Order Attributes | 统称，含**订单性质**（私密/公开）、**预算区间**、**工作量**等；用于订单展示与订单广场筛选，与发布模板字段一致或扩展。 |
| 指定接单方 | Assigned Contractor | 客户在创建订单时指定某用户（或运营方）为接单方；该订单仅对被指定方可见/可接。 |
| 平台运营方接单 | Operation-assigned Order | 私密订单的一种接单方式：客户指定由平台运营方承接该订单。 |
| 公开抢单 | Open Bidding | 公开订单的一种接单方式：订单在订单广场展示，供用户报名/竞标。 |
| 未上架 | Not Listed | 推广中订单的展示状态之一：订单已审核通过但尚未在订单广场或对指定方形成有效展示（如指定接单方尚未确认接单）。 |
| 已上架 | Listed | 推广中订单的展示状态之一：订单已在订单广场展示（公开抢单）或已对指定接单方可见并可操作。 |
| 订单状态 | Order Status | 新建、待审核、已打回、推广中（含未上架/已上架）、协议签署中、交付中、已验收、待结算、已结算、已取消、已终止。 |

## 1.2 修订记录

| 版本 | 内容 | 负责人 | 更新时间 | 备注 |
|------|------|--------|----------|------|
| 1.0 | 初稿，基于《CSDN 订单管理系统整体方案》第 6.3 节及第 4.1 节「订单发布与审核」「订单状态流转」拆分 | — | 2025-03-07 | — |

## 1.3 背景和价值

**背景与痛点**：  
客户在订单广场发单需要经历「选类型→选模板→选创建模式→填内容→设属性与接单方式→提交审核」的完整流程。若必填信息不统一（结算模式、订单交付模式、报价、接单要求、响应/招标时限等）、订单属性与接单方式不清晰，会导致审核标准不一、订单发布后展示与接单逻辑混乱；同时，指定接单方与公开抢单的超时行为、时限可调整时机若未明确，会影响客户与接单方的体验及运营处理口径。

**业务价值**：  
1. **发单流程标准化**：客户按「任务类型→可选模板→一种创建模式→必填信息→订单属性与接单方式」顺序提交，审核后按属性与接单方式发布，形成可复用的发单—审核—发布闭环。  
2. **结算模式与定义权清晰**：客户在定义订单时确定结算模式（平台抽佣或平台承包转包），明确里程碑与结算由客户还是运营方定义，为后续里程碑与结算需求提供依据。  
3. **响应/招标时限与超时行为可配置**：指定接单方订单配置响应时限、公开抢单配置招标时限，支持预设或无限制；超时后可配置自动下架或转为公开抢单/自动隐藏，并在待审核、未上架、推广中（已上架）状态下支持客户调整时限。  
4. **订单状态与审核可追溯**：订单具备完整状态流转，运营审核通过/打回及打回原因客户可见，审核通过后系统按订单属性与接单方式自动处理（上架订单广场或对指定方开放），便于运营与客户协同。

---

# 第2章：功能需求

## 2.1 发单流程与订单创建

### 场景描述

**场景 1：客户按流程完整发单**  
客户在订单创建页先选择**任务类型**（文本创作、视频创作、软件开发、产品评测、营销推广、平面设计、硬件订单之一），可选选择该类型下的**订单模板**，再选择**一种创建模式**（手工创建、上传文档 AI 解析创建、自然语言 AI 解析创建）。按所选模式填写或生成订单内容后，必填**结算模式**、**订单交付模式**、**订单报价**、**接单要求**及**响应时限/招标时限**（指定接单方为响应时限，公开抢单为招标时限），确认或修改草稿后**设置订单属性与接单方式**（含指定接单方未响应/拒绝时的处理、公开抢单超时处理），最后提交审核，订单状态由「新建」→「待审核」。

**场景 2：三种创建模式与模板结合**  
手工创建时按模板或默认表单项逐项填写；上传文档由 AI 解析创建时上传 Word、PDF 等，后台 AI 解析并生成订单草稿，客户确认或修改后提交；自然语言 AI 解析创建时客户用自然语言描述需求，后台 AI 解析并生成草稿，客户确认或修改后提交。三种模式均可与订单模板结合：选择模板后，手工模式按模板字段填；两种 AI 模式生成的草稿按模板结构填充，客户可在此基础上修改。

**场景 3：订单属性与接单方式组合**  
客户设置**订单性质**为私密或公开；私密订单接单方式可为**指定接单方**或**指定由平台运营方接单**；公开订单接单方式可为**指定接单方**或**公开抢单**。同时配置响应时限/招标时限及超时行为（指定接单方未响应/拒绝→自动下架或转为公开抢单；公开抢单超时→自动下架或超时订单自动隐藏）。

### 2.1.1 基本事件流程

#### 主业务流程

**前置条件**：操作人为已认证客户；系统已配置任务类型（前 7 种对客户可见）及可选发布模板；客户已选择任务类型。

**流程描述**：  
1. **选择任务类型**：客户在发单页选择任务类型（文本创作、视频创作、软件开发、产品评测、营销推广、平面设计、硬件订单之一）。  
2. **可选选择订单模板**：客户可选选择该类型下的某订单模板；若未选，按默认或该类型下默认模板展示表单项。  
3. **选择创建模式**：客户选择**手工创建**、**上传文档由 AI 解析创建**或**自然语言 AI 解析创建**之一。  
4. **填写或生成订单内容**：  
   - **手工创建**：按模板或默认表单项逐项填写订单内容（含订单交付模式、订单报价、接单要求及标题、描述、工作量、交付要求等，与发布模板字段一致）。  
   - **上传文档 AI 解析创建**：客户上传文档（如 Word、PDF），后台 AI 解析并生成订单草稿，草稿按所选模板结构填充；客户确认或修改后进入下一步。  
   - **自然语言 AI 解析创建**：客户输入自然语言描述需求，后台 AI 解析并生成订单草稿，草稿按所选模板结构填充；客户确认或修改后进入下一步。  
5. **必填信息**：客户须填写**结算模式**（二选一：平台抽佣 / 平台承包转包）、**订单交付模式**、**订单报价**、**接单要求**及**响应时限/招标时限**（指定接单方订单配置响应时限，公开抢单订单配置招标时限；支持常用预设如 3 天、7 天、15 天或**无限制**）。  
6. **设置订单属性与接单方式**：  
   - 设置**订单性质**（私密订单 / 公开订单）。  
   - 选择**接单方式**：私密订单可选**指定接单方**或**指定由平台运营方接单**；公开订单可选**指定接单方**或**公开抢单**。  
   - 若为**指定接单方**：选择平台内某用户（或运营方），并配置**响应时限**及「时限内未响应或拒绝」时的处理（**自动下架**或**自动转为公开抢单**）。  
   - 若为**公开抢单**：配置**招标时限**及「招标时限超时」时的处理（**自动下架**或**超时订单自动隐藏**）。  
7. **提交审核**：客户点击提交，系统校验必填项与格式（含结算模式、订单交付模式、订单报价、接单要求、响应时限/招标时限及订单属性与接单方式）；校验通过后订单状态由「新建」→「待审核」。

**后置条件**：订单已提交，状态为**待审核**；运营方可见待审核列表并可进行通过/打回操作。

**系统响应与提示**：  
- 必填项未填或校验不通过时，提示具体错误项（如「请选择结算模式」「请填写订单报价」「请配置响应时限」），不提交。  
- 提交成功：提示「订单已提交审核」或 Toast；订单在客户订单看板中显示为「待审核」。

#### 扩展事件流程

- **保存草稿**：客户在未提交前可保存草稿，订单保持「新建」状态；再次进入可继续编辑。具体是否支持草稿保存及草稿有效期，原始需求未明确，**需补充确认**。  
- **编辑后重提**：订单被运营打回（**已打回**）后，客户修改内容可再次提交，订单状态由「已打回」→「待审核」。

#### 异常事件流程

- **必填项缺失**：提交时校验，提示「请完善必填项」并定位到具体字段。  
- **AI 解析失败**（上传文档/自然语言模式）：提示「解析失败，请改用手工填写或重新上传/输入」，不阻塞发单。  
- **指定接单方未选择用户**：若接单方式为指定接单方，须选择平台内某用户；未选择时提示「请选择接单方」。

### 2.1.2 数据项描述（发单流程与订单主信息）

以下为业务与逻辑层面数据项；具体表结构、字段类型与长度由实现设计确定。订单内容字段（订单交付模式、订单报价、接单要求、标题、描述、工作量、交付要求等）与发布模板字段一致，参见《订单发布模板与任务类型管理》需求文档；此处仅列出本功能涉及的订单级字段。

| 字段名（中文） | 字段名（英文） | 数据类型/长度 | 是否必填 | 前端展示 | 默认值 | 说明 | 界面/控件说明 | 备注 |
|----------------|----------------|----------------|----------|----------|--------|------|----------------|------|
| 订单标识 | order_id | 唯一标识 | — | 否 | — | 订单主键 | — | — |
| 任务类型 | task_type_code | 字符串，与任务类型一致 | 是 | 是 | — | 客户所选任务类型（前 7 种之一） | 下拉/单选 | — |
| 订单模板 | template_id | 引用 | 否 | 是 | — | 客户可选该类型下某模板 | 下拉/可选 | — |
| 创建模式 | creation_mode | 枚举 | 是 | 是 | — | manual / document_ai / nl_ai | 创建页模式选择 | — |
| 结算模式 | settlement_mode | 枚举 | 是 | 是 | — | platform_commission / platform_contract；平台抽佣 / 平台承包转包 | 二选一 | — |
| 订单交付模式 | delivery_mode | 与模板一致 | 是 | 是 | — | 必填，与发布模板字段一致 | 表单项 | — |
| 订单报价 | order_quote | 与模板一致 | 是 | 是 | — | 必填，格式与校验与模板一致 | 表单项 | — |
| 接单要求 | contractor_requirements | 与模板一致 | 是 | 是 | — | 必填 | 表单项 | — |
| 订单性质 | order_nature | 枚举 | 是 | 是 | — | private / public；私密订单 / 公开订单 | 二选一 | — |
| 接单方式 | accept_mode | 枚举 | 是 | 是 | — | assigned_contractor / operation_accept / open_bidding；指定接单方 / 运营方接单 / 公开抢单；私密仅前两种，公开可为指定接单方或公开抢单 | 根据订单性质展示可选值 | — |
| 指定接单方 | assigned_contractor_id | 用户/运营引用 | 条件必填 | 是 | — | 接单方式为「指定接单方」时必选 | 用户选择器 | — |
| 响应时限 | response_deadline | 枚举或时长 | 条件必填 | 是 | — | 指定接单方订单必配；常用预设（如 3 天、7 天、15 天）或无限制；可调整时机见 2.3 | 下拉/选择 | — |
| 招标时限 | bidding_deadline | 枚举或时长 | 条件必填 | 是 | 是 | 公开抢单订单必配；常用预设或无限制；可调整时机见 2.3 | 下拉/选择 | — |
| 指定接单方超时处理 | assigned_timeout_action | 枚举 | 条件必填 | 是 | — | 指定接单方订单：auto_off_shelf / turn_to_open_bidding；自动下架 / 自动转为公开抢单 | 二选一 | — |
| 公开抢单超时处理 | bidding_timeout_action | 枚举 | 条件必填 | 是 | — | 公开抢单订单：auto_off_shelf / auto_hide；自动下架 / 超时订单自动隐藏；具体采用下架或隐藏在业务确认后确定 | 二选一 | — |
| 订单状态 | order_status | 枚举 | 是 | 是 | 新建 | 新建、待审核、已打回、推广中、协议签署中、交付中、已验收、待结算、已结算、已取消、已终止 | 状态标签 | — |
| 上架状态 | list_status | 枚举 | — | 是（推广中时） | — | 推广中阶段：未上架 / 已上架 | 状态或标签 | — |

### 2.1.3 需求波及分析

- **影响模块**：客户发单页（任务类型、模板、创建模式、必填信息、订单属性与接单方式）、订单草稿与提交校验、运营审核列表与订单发布逻辑。  
- **数据影响**：订单主表及与任务类型、模板、客户、指定接单方的关联；必填字段与校验规则与发布模板字段统一配置。  
- **业务规则影响**：发单流程顺序、必填信息范围、订单性质与接单方式组合规则、响应/招标时限及超时行为配置。  
- **历史文档查阅记录**：  
  - 查阅的整体方案文档：`CSDN订单系统/docs/CSDN订单管理系统整体方案.md`（工作区相对路径）  
  - 参考功能：第 6.3 节「客户发布订单与审核」、第 4.1 节「订单发布与审核」「订单状态流转」、术语表  
  - 设计一致性保证：发单流程、必填信息、订单属性与接单方式、状态流转与整体方案表述一致。  
  - 查阅的现有功能文档：`CSDN订单系统/docs/订单发布模板与任务类型管理产品需求说明书.md`；参考实现：任务类型与模板选择、三种创建模式与模板结合、必含字段（订单交付模式、订单报价、接单要求）与校验规则，本需求中订单内容字段与模板字段统一，不重复定义。  
  - 本项目 `docs/history/` 下未发现「客户发布订单与审核」专项历史需求文档，以整体方案及同项目《订单发布模板与任务类型管理》为设计来源。

---

## 2.2 订单必填信息与结算模式

### 场景描述

客户发布订单时须填写**结算模式**、**订单交付模式**、**订单报价**、**接单要求**及**响应时限/招标时限**（指定接单方订单为响应时限，公开抢单订单为招标时限）。**结算模式**二选一：**平台抽佣**（客户与用户直接交易结算，由客户定义该订单的里程碑、结算等）或**平台承包转包**（客户将订单承包给平台、平台转包给用户，由平台运营方定义该订单的里程碑、结算等）。具体字段含义、格式与校验规则在需求文档中定义，可与发布模板字段统一配置。

### 2.2.1 基本事件流程

#### 主业务流程

**前置条件**：客户已选择任务类型与创建模式，并完成订单内容填写或草稿生成（手工或 AI 解析）。

**流程描述**：  
1. **结算模式**：客户在订单表单中选择**平台抽佣**或**平台承包转包**；系统记录该选择，用于后续里程碑与结算由谁定义的业务规则。  
2. **订单交付模式、订单报价、接单要求**：客户按模板或表单项填写；系统按发布模板配置执行必填与校验（格式、长度、数值范围等）。  
3. **响应时限/招标时限**：若接单方式为**指定接单方**，客户配置**响应时限**（常用预设如 3 天、7 天、15 天或**无限制**）；若接单方式为**公开抢单**，客户配置**招标时限**（同样支持预设或无限制）。  
4. 上述必填项均通过校验后，客户方可提交审核。

**后置条件**：订单记录中结算模式、订单交付模式、订单报价、接单要求、响应时限/招标时限已持久化；后续审核、发布、里程碑定义方逻辑依赖结算模式。

**系统响应与提示**：  
- 未选择结算模式：提示「请选择结算模式（平台抽佣或平台承包转包）」。  
- 订单报价等不符合校验：提示具体规则（如「订单报价须大于 0」）。

#### 异常事件流程

- **结算模式未选**：提交时校验，提示「请选择结算模式」。  
- **订单报价/接单要求等违反模板校验**：与《订单发布模板与任务类型管理》中模板字段校验一致，提示具体错误项并阻止提交。

### 2.2.2 需求波及分析

- **影响模块**：订单创建表单、订单存储、后续「里程碑确认与交付验收」中里程碑定义方（客户 vs 运营方）逻辑。  
- **数据影响**：订单表必填字段；与发布模板字段统一配置，不重复定义格式与校验。  
- **业务规则影响**：结算模式二选一且必填；订单交付模式、订单报价、接单要求为必填并与模板一致。  
- **历史文档查阅记录**：同 2.1.3；本功能对应整体方案 6.3.2「订单必填信息」「结算模式」。

---

## 2.3 响应时限与招标时限的配置及调整

### 场景描述

客户新建订单时需配置——**指定接单方**订单配置**响应时限**（被指定方需在时限内响应或拒绝），**公开抢单**订单配置**招标时限**（订单在订单广场接受报名的时长）；支持常用时限预设（如 3 天、7 天、15 天等）或**无限制**。该时限可在订单处于**待审核**、**未上架**（推广中且未上架）、**推广中（已上架）**时由客户灵活调整，具体可调整次数与校验在需求文档中定义。

### 2.3.1 基本事件流程

#### 主业务流程

**前置条件**：订单已创建且接单方式为指定接单方或公开抢单；订单状态为**待审核**、**推广中（未上架）**或**推广中（已上架）**。

**流程描述**：  
1. **新建时配置**：客户在发单时为指定接单方订单选择响应时限（如 7 天或无限制），为公开抢单订单选择招标时限（如 15 天或无限制）。  
2. **调整时机**：客户在订单详情或订单管理入口，当订单状态为**待审核**、**未上架**（推广中且未上架）、**推广中（已上架）**时，可进入「修改响应时限」或「修改招标时限」进行修改。  
3. **保存**：客户修改后保存，系统校验时限取值合法（在预设枚举内或无限制）；校验通过后更新订单的响应时限/招标时限。  

**后置条件**：订单的响应时限或招标时限已更新；若已上架公开抢单订单修改招标时限，新时限自保存起生效（具体是否按「剩余时长」或「新的绝对截止时间」由业务确认，**此处信息不明确，需补充确认**）。

**系统响应与提示**：  
- 若业务限制可调整次数：达到次数上限时提示「响应时限/招标时限已达最大调整次数，无法再修改」。  
- 保存成功：提示「时限已更新」。

#### 扩展事件流程

- **可调整次数**：具体可调整次数与校验在需求文档中定义；若原始需求未明确，实现前需与业务确认（如每单最多调整 3 次）。  

#### 异常事件流程

- **状态不允许调整**：订单已进入协议签署中、交付中等状态后，响应时限/招标时限不再开放调整，入口不展示或操作时提示「当前状态不可修改时限」。

### 2.3.2 需求波及分析

- **影响模块**：订单创建页、订单详情/订单管理（客户侧）、指定接单方响应倒计时/公开抢单招标倒计时逻辑。  
- **数据影响**：订单表响应时限、招标时限字段及可选「调整次数」计数（若业务要求）。  
- **业务规则影响**：允许调整的状态为待审核、未上架、推广中（已上架）；可调整次数与校验需与业务确认。  
- **历史文档查阅记录**：同 2.1.3；本功能对应整体方案 6.3.2「响应时限与招标时限」、4.1「响应时限/招标时限可在…由客户灵活调整」。

---

## 2.4 上架状态与超时行为

### 场景描述

**推广中**阶段区分**未上架**/**已上架**：如指定接单方尚未确认为未上架，公开抢单已展示于订单广场为已上架。指定接单方订单可配置：时限内未响应或拒绝 → **自动下架**或**自动转为公开抢单**。公开抢单订单可配置：招标时限超时 → **自动下架**或**超时订单自动隐藏**（不再在广场展示），具体采用下架或隐藏在需求文档中确定。

### 2.4.1 基本事件流程

#### 主业务流程

**前置条件**：订单已审核通过，状态为**推广中**；系统按接单方式与业务规则判定**未上架**或**已上架**。

**流程描述**：  
1. **未上架/已上架判定**：  
   - **指定接单方**订单：指定方尚未确认接单或拒绝前为**未上架**；指定方确认接单后为**已上架**（对该订单而言已确定接单方）。  
   - **公开抢单**订单：审核通过并展示于订单广场后为**已上架**；未展示前为**未上架**（通常审核通过即展示，故多数情况下为已上架）。  
2. **指定接单方超时**：若被指定方在**响应时限**内未响应或拒绝，系统按订单配置执行**自动下架**（订单状态可由推广中→已取消或标记为下架）或**自动转为公开抢单**（接单方式变为公开抢单，按招标时限在订单广场展示，供用户报名/竞标）。  
3. **公开抢单超时**：招标时限到期后，系统按订单配置执行**自动下架**或**超时订单自动隐藏**（订单不再在订单广场展示）；具体采用「下架」还是「隐藏」以与业务确认为准，**此处信息不明确，需补充确认**。

**后置条件**：超时后订单已按配置执行下架或转为公开抢单/隐藏；订单广场仅展示未超时且未下架/未隐藏的公开抢单订单。

**系统响应与提示**：  
- 指定接单方超时后若转为公开抢单：客户与运营可见「已转为公开抢单，招标时限为 X」；订单在订单广场展示。  
- 公开抢单超时后：订单在订单广场不再展示；客户在订单看板中可见订单状态或「已下架/已隐藏」标记。

#### 异常事件流程

- **定时任务延迟**：超时检测若依赖定时任务，可能存在分钟级延迟；若业务要求精确到秒，需在实现时说明。

### 2.4.2 需求波及分析

- **影响模块**：订单状态机、订单广场展示逻辑、指定接单方响应倒计时、公开抢单招标倒计时与超时任务。  
- **数据影响**：订单状态、上架状态、接单方式（转为公开抢单时更新）、订单广场可见性。  
- **业务规则影响**：未上架/已上架判定规则；指定接单方超时→自动下架或转为公开抢单；公开抢单超时→自动下架或自动隐藏。  
- **历史文档查阅记录**：同 2.1.3；本功能对应整体方案 6.3.2「上架状态与超时行为」。

---

## 2.5 订单状态与流转

### 场景描述

订单状态包括**新建**、**待审核**、**已打回**、**推广中**（含未上架/已上架）、**协议签署中**、**交付中**、**已验收**、**待结算**、**已结算**、**已取消**、**已终止**。提交后由运营方审核；审核通过 → **推广中**，不通过 → **已打回**（客户修改后可再提交 → 待审核）。**待审核**、**推广中**可由客户或运营方取消 → **已取消**；取消为逻辑删除，报名记录保留。选标/确认接单后 → **协议签署中**；协议签署并上传至平台（自动关联订单）→ **交付中**。**交付中**若某里程碑超期未达成则开放客户终止权 → **已终止**；终止后结算以双方协议为准，由运营人工审核并按条款执行，平台不自动结算。指定接单方在响应时限内未响应或拒绝时，按配置自动下架或转为公开抢单；公开抢单招标时限超时后按配置自动下架或自动隐藏。全部里程碑通过 → **已验收** → **待结算** → **已结算**。

### 2.5.1 基本事件流程

#### 主业务流程

**前置条件**：订单已创建并提交（状态为待审核）或已处于后续任意状态。

**流程描述**（与整体方案第 4 章一致）：  
1. **新建** → 客户提交审核 → **待审核**。  
2. **待审核** → 运营通过 → **推广中**；运营打回 → **已打回**（客户可修改后再提交 → 待审核）。客户或运营取消 → **已取消**。  
3. **推广中**：区分未上架/已上架；客户或运营取消 → **已取消**。指定接单方超时未响应或拒绝 → 按配置**自动下架**或**转为公开抢单**；公开抢单招标超时 → 按配置**自动下架**或**自动隐藏**。选标/确认接单 → **协议签署中**。  
4. **协议签署中** → 协议签署并上传至平台（自动关联订单）→ **交付中**。  
5. **交付中** → 全部里程碑通过 → **已验收**；某里程碑超期未达成则开放客户终止权，客户行使终止 → **已终止**。  
6. **已验收** → **待结算** → **已结算**。  
7. **已取消**：逻辑删除，报名记录保留。

**后置条件**：订单状态与整体方案定义一致；本需求重点覆盖至「审核通过→推广中」及「待审核/推广中取消」，其余状态在《订单广场与报名竞标》《里程碑确认与交付验收》等需求中细化。

**系统响应与提示**：  
- 客户提交后：订单状态变更为待审核，客户端提示「订单已提交审核」。  
- 运营打回：客户可见打回原因，订单状态为已打回。  
- 取消订单：确认弹窗「确定取消该订单？取消后报名记录保留」；确认后状态为已取消。

#### 扩展事件流程

- **已打回后重提**：客户修改后再次提交，订单由已打回 → 待审核。  

#### 异常事件流程

- **重复提交**：订单已为待审核时，不允许重复提交，或提示「订单已在审核中」。

### 2.5.2 需求波及分析

- **影响模块**：订单状态机、客户订单看板、运营审核与订单列表、订单广场展示、后续协议签署与交付流程。  
- **数据影响**：订单状态字段、上架状态字段、逻辑删除标记。  
- **业务规则影响**：完整状态枚举与流转与整体方案第 4 章一致；本需求文档不改变方案已定的状态定义。  
- **历史文档查阅记录**：同 2.1.3；本功能对应整体方案 4.1「订单状态流转」、6.3.2「订单状态与流转」。

---

## 2.6 运营审核操作

### 场景描述

运营方对客户提交的订单进行**通过**或**打回**操作。打回时需填写原因，客户可见。审核通过后，系统按订单属性与接单方式处理：公开抢单的订单上架订单广场；指定接单方的订单仅对指定用户开放；运营方接单的订单由运营方在后台承接。

### 2.6.1 基本事件流程

#### 主业务流程

**前置条件**：操作人为运营方，已登录运营后台并具备订单审核权限；存在状态为**待审核**的订单。

**流程描述**：  
1. **进入待审核列表**：运营在后台通过「订单管理」「待审核」或业务看板进入待审核订单列表。  
2. **查看订单详情**：运营点击某订单查看完整内容（任务类型、模板、创建模式、结算模式、订单交付模式、订单报价、接单要求、响应时限/招标时限、订单性质、接单方式及指定接单方等）。  
3. **审核操作**：  
   - **通过**：运营点击「通过」，系统校验订单数据完整（必填项均已填、接单方式与指定接单方/运营方接单一致）；校验通过后订单状态由**待审核** → **推广中**。系统按订单属性与接单方式处理：**公开抢单**的订单上架订单广场；**指定接单方**的订单仅对指定用户开放（如「指定给我的订单」入口）；**指定由平台运营方接单**的订单出现在运营端待承接列表，由运营方在后台承接。  
   - **打回**：运营点击「打回」，需填写**打回原因**（必填）；保存后订单状态由**待审核** → **已打回**，客户在订单看板或订单详情中可见该订单状态为「已打回」及打回原因。  
4. **客户可见性**：打回原因在客户侧订单详情或通知中展示，客户可据此修改后重新提交。

**后置条件**：  
- 通过：订单为**推广中**，公开抢单订单已在订单广场展示，指定接单方订单对指定用户可见，运营方接单订单在运营端可承接。  
- 打回：订单为**已打回**，客户可见原因并可修改后再提交。

**系统响应与提示**：  
- 打回时未填写原因：提示「请填写打回原因」，不提交打回操作。  
- 审核通过成功：提示「审核通过，订单已发布」或 Toast；待审核列表该订单移除。  
- 打回成功：提示「已打回」；客户端展示打回原因。

#### 扩展事件流程

- **批量审核**：原始需求未提及批量通过/打回，若业务需要可在后续需求中补充。  

#### 异常事件流程

- **订单已被取消**：若客户或运营在审核前已取消订单，订单状态为已取消，运营端待审核列表不应再展示该订单，或展示时提示「订单已取消」。  
- **权限不足**：非运营或未具备审核权限的用户无法访问待审核列表或执行通过/打回。

### 2.6.2 数据项描述（审核相关）

| 字段名（中文） | 字段名（英文） | 数据类型/长度 | 是否必填 | 前端展示 | 默认值 | 说明 | 界面/控件说明 | 备注 |
|----------------|----------------|----------------|----------|----------|--------|------|----------------|------|
| 审核结果 | audit_result | 枚举 | 是 | 是 | — | pass / reject；通过 / 打回 | — | — |
| 打回原因 | reject_reason | 文本，建议 500 | 条件必填 | 是（客户可见） | — | 打回时必填，客户可见 | 多行输入（运营）；只读展示（客户） | — |
| 审核人 | auditor_id | 引用 | 是 | 是（运营端） | — | 执行审核操作的运营账号 | — | — |
| 审核时间 | audit_time | 时间戳 | 是 | 是 | — | 通过或打回操作时间 | — | — |

### 2.6.3 需求波及分析

- **影响模块**：运营端订单审核列表与详情、通过/打回操作、订单状态更新、订单广场上架逻辑、指定接单方可见性、运营端待承接订单列表；客户订单看板与订单详情（打回原因展示）。  
- **数据影响**：订单审核记录表或订单表审核相关字段；订单状态与上架状态更新。  
- **业务规则影响**：打回必填原因、客户可见；审核通过后按订单属性与接单方式自动处理发布。  
- **历史文档查阅记录**：同 2.1.3；本功能对应整体方案 6.3.2「审核操作」。

---

## 2.7 验收准则

### 2.7.1 验收准则表格

| 编号 | 场景描述 | Given（前置条件） | When（触发条件） | Then（预期结果） | And（附加验证） |
|------|----------|-------------------|-------------------|------------------|-----------------|
| AC-1 | 客户发单完整流程可走通 | 客户已认证，任务类型与模板已配置 | 客户选择任务类型（前 7 种之一）、可选模板、一种创建模式，填写必填信息并设置订单属性与接单方式后提交 | 系统校验通过，订单状态变为「待审核」 | 运营端待审核列表可见该订单 |
| AC-2 | 结算模式与必填项必填 | 客户在发单页已选择任务类型与创建模式并完成内容填写 | 客户未选择结算模式或未填写订单报价/接单要求/响应时限或招标时限即点击提交 | 系统提示必填项未填（如「请选择结算模式」「请配置响应时限」），不提交 | 客户补充后再次提交可成功 |
| AC-3 | 指定接单方订单须配置响应时限及超时处理 | 客户选择接单方式为「指定接单方」并选择某用户 | 客户未配置响应时限或未选择「未响应/拒绝时」的处理方式（自动下架或转为公开抢单）即提交 | 系统提示请配置响应时限及超时处理，不提交 | 配置完整后可提交 |
| AC-4 | 公开抢单订单须配置招标时限及超时处理 | 客户选择接单方式为「公开抢单」 | 客户未配置招标时限或未选择超时处理（自动下架或自动隐藏）即提交 | 系统提示请配置招标时限及超时处理，不提交 | 配置完整后可提交 |
| AC-5 | 订单性质与接单方式组合正确 | 客户设置订单性质为私密 | 客户选择接单方式 | 系统仅展示「指定接单方」「指定由平台运营方接单」 | 公开订单时展示「指定接单方」「公开抢单」 |
| AC-6 | 运营打回须填写原因且客户可见 | 存在待审核订单，运营具备审核权限 | 运营点击「打回」但未填写打回原因 | 系统提示「请填写打回原因」，不提交打回 | 填写原因后打回成功，客户在订单详情可见打回原因 |
| AC-7 | 审核通过后按接单方式处理 | 运营对某待审核订单点击「通过」 | 该订单为公开抢单 | 订单状态变为「推广中」，订单在订单广场展示 | 指定接单方订单仅对指定用户开放；运营方接单订单在运营端可承接 |
| AC-8 | 待审核或推广中可取消且为逻辑删除 | 订单状态为待审核或推广中 | 客户或运营方执行取消订单 | 订单状态变为「已取消」 | 报名记录保留，取消为逻辑删除 |
| AC-9 | 响应时限/招标时限可在规定状态下调整 | 订单状态为待审核或推广中（未上架或已上架） | 客户在订单详情或管理入口修改响应时限或招标时限并保存 | 系统更新订单的响应时限或招标时限 | 若业务限制调整次数，达上限时提示不可再修改 |
| AC-10 | 指定接单方超时后按配置执行 | 订单为指定接单方，已配置「未响应/拒绝时转为公开抢单」 | 被指定方在响应时限内未响应也未拒绝 | 系统将订单转为公开抢单，按招标时限在订单广场展示 | 若配置为自动下架，则订单下架不再接受报名 |
| AC-11 | 公开抢单超时后按配置执行 | 订单为公开抢单，已配置超时后「自动隐藏」 | 招标时限到期 | 订单在订单广场不再展示（自动隐藏或下架） | 客户在订单看板仍可见该订单及状态 |

### 2.7.2 Gherkin 格式验收准则

```gherkin
Feature: 客户发布订单与审核

  Scenario: 客户发单完整流程可走通
    Given 客户已认证
    And 任务类型与模板已配置
    When 客户选择任务类型（前 7 种之一）
    And 客户可选选择订单模板
    And 客户选择一种创建模式（手工/上传文档AI/自然语言AI）
    And 客户填写必填信息（结算模式、订单交付模式、订单报价、接单要求、响应时限或招标时限）
    And 客户设置订单属性与接单方式
    And 客户点击提交
    Then 系统校验通过
    And 订单状态变为「待审核」
    And 运营端待审核列表可见该订单

  Scenario: 结算模式与必填项必填
    Given 客户在发单页已选择任务类型与创建模式并完成内容填写
    When 客户未选择结算模式即点击提交
    Then 系统提示「请选择结算模式」或类似必填提示
    And 系统不提交订单
    When 客户未填写订单报价或接单要求或未配置响应时限/招标时限即提交
    Then 系统提示对应必填项未填
    And 系统不提交
    When 客户补充全部必填项后再次提交
    Then 提交成功

  Scenario: 指定接单方订单须配置响应时限及超时处理
    Given 客户选择接单方式为「指定接单方」并选择某用户
    When 客户未配置响应时限即点击提交
    Then 系统提示请配置响应时限，不提交
    When 客户未选择「未响应/拒绝时」的处理方式（自动下架或转为公开抢单）即提交
    Then 系统提示请选择超时处理方式，不提交
    When 客户配置响应时限及超时处理方式后提交
    Then 提交成功

  Scenario: 公开抢单订单须配置招标时限及超时处理
    Given 客户选择接单方式为「公开抢单」
    When 客户未配置招标时限或未选择超时处理即提交
    Then 系统提示请配置招标时限及超时处理，不提交
    When 客户配置招标时限及超时处理（自动下架或自动隐藏）后提交
    Then 提交成功

  Scenario: 订单性质与接单方式组合正确
    Given 客户设置订单性质为「私密订单」
    When 客户查看接单方式选项
    Then 系统仅展示「指定接单方」「指定由平台运营方接单」
    Given 客户设置订单性质为「公开订单」
    When 客户查看接单方式选项
    Then 系统展示「指定接单方」「公开抢单」

  Scenario: 运营打回须填写原因且客户可见
    Given 存在待审核订单
    And 运营具备审核权限
    When 运营点击「打回」
    And 运营未填写打回原因即确认
    Then 系统提示「请填写打回原因」
    And 打回操作不提交
    When 运营填写打回原因后确认打回
    Then 订单状态变为「已打回」
    And 客户在订单详情或订单看板可见该订单状态为「已打回」及打回原因

  Scenario: 审核通过后按接单方式处理
    Given 存在待审核的公开抢单订单
    When 运营点击「通过」
    Then 订单状态变为「推广中」
    And 该订单在订单广场展示
    Given 存在待审核的指定接单方订单
    When 运营点击「通过」
    Then 订单状态变为「推广中」
    And 该订单仅对指定用户开放（如指定给我的订单入口可见）
    Given 存在待审核的运营方接单订单（私密且指定由平台运营方接单）
    When 运营点击「通过」
    Then 订单状态变为「推广中」
    And 该订单在运营端待承接列表中可被运营方承接

  Scenario: 待审核或推广中可取消且为逻辑删除
    Given 订单状态为「待审核」或「推广中」
    When 客户或运营方执行取消订单操作并确认
    Then 订单状态变为「已取消」
    And 报名记录保留在数据库中
    And 取消为逻辑删除

  Scenario: 响应时限/招标时限可在规定状态下调整
    Given 订单状态为「待审核」或「推广中（未上架）」或「推广中（已上架）」
    When 客户在订单详情或订单管理入口修改响应时限或招标时限并保存
    Then 系统更新订单的响应时限或招标时限
    And 保存成功提示
    When 业务规则限制调整次数且该订单已达调整次数上限
    And 客户再次尝试修改时限
    Then 系统提示不可再修改或已达最大调整次数

  Scenario: 指定接单方超时后按配置执行
    Given 订单为指定接单方且已配置「未响应/拒绝时」为「自动转为公开抢单」
    And 已配置招标时限
    When 被指定方在响应时限内未响应也未拒绝
    Then 系统将订单接单方式变为公开抢单
    And 订单按招标时限在订单广场展示
    Given 订单为指定接单方且已配置「未响应/拒绝时」为「自动下架」
    When 被指定方在响应时限内未响应也未拒绝
    Then 订单被自动下架，不再接受报名

  Scenario: 公开抢单超时后按配置执行
    Given 订单为公开抢单且已配置超时后「自动隐藏」（或自动下架）
    When 招标时限到期
    Then 订单在订单广场不再展示
    And 客户在订单看板仍可见该订单及状态（已下架/已隐藏等）
```

---

## 2.8 国际化命名规则

| 使用场景说明 | 中文 | 英文 |
|--------------|------|------|
| 发单 | 发布订单 | Publish Order |
| 任务类型 | 任务类型 | Task Type |
| 订单模板 | 订单模板 | Order Template |
| 创建模式 | 手工创建 | Manual Creation |
| 创建模式 | 上传文档由 AI 解析创建 | Create from Document (AI) |
| 创建模式 | 自然语言 AI 解析创建 | Create from Description (AI) |
| 结算模式 | 平台抽佣 | Platform Commission |
| 结算模式 | 平台承包转包 | Platform Contract |
| 订单性质 | 私密订单 | Private Order |
| 订单性质 | 公开订单 | Public Order |
| 接单方式 | 指定接单方 | Assigned Contractor |
| 接单方式 | 平台运营方接单 | Operation Assigned |
| 接单方式 | 公开抢单 | Open Bidding |
| 响应时限 | 响应时限 | Response Deadline |
| 招标时限 | 招标时限 | Bidding Deadline |
| 超时处理 | 自动下架 | Auto Off Shelf |
| 超时处理 | 自动转为公开抢单 | Turn to Open Bidding |
| 超时处理 | 超时订单自动隐藏 | Auto Hide When Timeout |
| 订单状态 | 新建 | Draft |
| 订单状态 | 待审核 | Pending Review |
| 订单状态 | 已打回 | Rejected |
| 订单状态 | 推广中 | Promoting |
| 订单状态 | 未上架 | Not Listed |
| 订单状态 | 已上架 | Listed |
| 审核 | 通过 | Approve |
| 审核 | 打回 | Reject |
| 打回原因 | 打回原因 | Reject Reason |
| 通用 | 提交 | Submit |
| 通用 | 取消 | Cancel |

---

## 2.9 埋点定义

| 模块 | 指标名称 | 指标定义 | PC/移动端 | 触发时机 | 频率 |
|------|----------|----------|------------|----------|------|
| 发单 | 发单页曝光 | 客户进入发布订单页 | 均可 | 页面曝光 | 按次 |
| 发单 | 任务类型选择 | 客户选择任务类型 | 均可 | 选择并确认 | 按次 |
| 发单 | 创建模式选择 | 客户选择手工/上传文档AI/自然语言AI | 均可 | 选择模式 | 按次 |
| 发单 | 订单提交成功 | 客户提交订单且校验通过 | 均可 | 提交成功 | 按次 |
| 发单 | 订单提交失败 | 客户提交订单但校验不通过 | 均可 | 校验失败提示 | 按次 |
| 审核 | 待审核列表曝光 | 运营端待审核订单列表被展示 | PC | 页面曝光 | 按次 |
| 审核 | 审核通过 | 运营对订单执行通过操作 | PC | 点击通过且成功 | 按次 |
| 审核 | 审核打回 | 运营对订单执行打回操作并填写原因 | PC | 点击打回且成功 | 按次 |
| 订单管理 | 响应/招标时限修改 | 客户修改响应时限或招标时限并保存 | 均可 | 保存成功 | 按次 |
| 订单管理 | 订单取消 | 客户或运营在待审核/推广中取消订单 | 均可 | 确认取消成功 | 按次 |

---

## 2.10 非功能性需求

- **性能**：订单提交（含必填校验）响应时间建议不超过 3 秒（正常网络条件下）；AI 解析创建订单的解析请求与《订单发布模板与任务类型管理》一致，超时时间建议不超过 60 秒。运营端待审核列表加载响应时间建议不超过 3 秒。  
- **安全**：仅已认证客户可发单；仅运营方具备订单审核权限；打回原因仅客户与运营可见，不向其他角色展示。  
- **兼容性**：客户发单与订单管理支持整体方案约定的网页端与移动端；运营审核以网页端为主；兼容方案约定的浏览器与操作系统。  
- **可用性**：必填项与校验规则与发布模板统一，提示文案明确（如「请选择结算模式」「请配置响应时限」）；审核打回原因客户必可见，便于修改后重提。

---

*本文档为《CSDN 订单管理系统整体方案》第 6.3 节「客户发布订单与审核」的细化需求说明书；响应时限/招标时限的可调整次数、公开抢单超时采用「下架」还是「隐藏」、转为公开抢单后招标时限的生效方式等需在实现前与业务补充确认。*
